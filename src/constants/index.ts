export enum Theme {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum AppRoutes {
  DISCOVER = '/',
  PLAYLIST = '/playlist',
  MOVIE = '/movies',
  TV_SHOWS = '/tv-shows',
  MY_LIST = '/my-list',
  WATCH_LATER = '/watch-later',
  RECOMMENDED = '/recommened',
  SETTINGS = '/settings',
  LOGOUT = '/logout',
}
